 CREATE DATABASE DV_BANCO
 USE DV_BANCO
create TABLE TICKET(
ID_TICKET INT PRIMARY KEY IDENTITY,
DESCRICAO VARCHAR (150),
UNIDADE VARCHAR (30),
CONTATO VARCHAR (30),
SETOR VARCHAR (30),
PRIORIDADE VARCHAR (20),
STATUS VARCHAR (20),
PATRIMONIO VARCHAR (10),
ABERTURA_POR VARCHAR (20),
DATA_ABERTURA DATE);

/*DATA_ABERTURA DATE*/

insert into TICKET VALUES ('teste','bahia','989831574','helpdesk','urgente','em atendimento',98987,'Victor','20/01/2020');

SELECT * FROM TICKET

 CREATE TABLE PESSOA(
ID INT PRIMARY KEY IDENTITY,
ID_P
NOME VARCHAR (30),
EMAIL VARCHAR (30),
UNIDADE VARCHAR (30),
TIPO VARCHAR (30),
CONTATO VARCHAR (20),
LOGIN VARCHAR (20),
SENHA INT );
FOREIGN KEY (COD_CLI)